{
  "name": "onboarding-backend",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds": "^9",
    "@sap/cds-dk": "^9",
    "dotenv": "^17.4.2",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.7",
    "sharp": "^0.34.5",
    "tesseract.js": "^7.0.0",
    "twilio": "^6.0.0"
  },
  "scripts": {
    "start": "npx cds serve --production"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "db.sqlite"
        }
      },
      "auth": {
        "kind": "mocked"
      },
      "http": {
        "body": {
          "limit": "10mb"
        }
      }
    },
    "auth": {
      "mocked": {
        "users": {
          "anyone": {
            "roles": []
          }
        }
      }
    }
  }
}
