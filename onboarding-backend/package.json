{
  "name": "onboarding-backend",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@sap/cds": "^9",
    "@sap/cds-dk": "^9",
    "@cap-js/sqlite": "^2",
    "dotenv": "^17.4.2",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.7",
    "twilio": "^6.0.0"
  },
  "scripts": {
    "start": "npx cds serve --production"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "db.sqlite"
        }
      },
      "auth": {
        "kind": "mocked"
      }
    },
    "auth": {
      "mocked": {
        "users": {
          "anyone": {
            "roles": []
          }
        }
      }
    }
  }
}
